Currently empty — all three typefaces load from Google Fonts:
Playfair Display, Literata, IBM Plex Mono.

To self-host (faster, no third-party request), download the woff2 files
here and replace the <link> in index.html with @font-face rules.
