Project images. Filenames must match those referenced in index.html:

  drawer-redesign.png
  patient-history.png    (missing — no image for this case study yet)
  sensecast.png
  mml.png
  portrait.jpg

The page tries the Framer URLs first, then falls back to these.
Any image that is missing hides its figure instead of showing a broken icon.
